import styled from "styled-components";
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';

export const StyledTaskEditIcon = styled(FontAwesomeIcon)`

  color: #333;
  cursor: pointer;
  
`;